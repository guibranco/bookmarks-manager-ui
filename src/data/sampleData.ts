import { Bookmark, Folder } from '../types';
import sampleData from './sample.json';

export const sampleFolders: Folder[] = sampleData.folders;
export const sampleBookmarks: Bookmark[] = sampleData.bookmarks;
